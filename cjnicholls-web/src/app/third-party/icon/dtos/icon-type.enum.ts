/**
 * Icon enum to type an interface for 3rd party icon libraries
 * `0` is skipped so it can be used to check for empty
 */
export enum IconTypeEnum {
    'github' = 1,
    'email',
    'linkedIn'
}